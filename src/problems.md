# Problems note

1. How to write a function to read file names and save them in a list?
2. I want to assign an ID to each To-Do list, then save their tasks in `.csv` files with the same name as the list's ID.
3. I want the lists to have custom names with emojis, etc. Then save the names in a `JSON` file. I can find list names using their ID in that `JSON` file.
4. There's a distinction between task loading and list loading!
5. How to work with JSON files?
6. I need to learn JSON syntak a bit
